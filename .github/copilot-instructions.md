# Copilot Instructions

Read `AGENTS.md` in the repository root for project rules (architecture, conventions, commands, gotchas).

## Copilot-specific notes

- Code reviews: check against Gotchas section in AGENTS.md
- Suggest fixes that follow the project's pre-commit checks (test → clippy → fmt)
- Rust 2021 edition, `cargo fmt` style
